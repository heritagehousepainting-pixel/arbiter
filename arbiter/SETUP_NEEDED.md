# Arbiter — SETUP NEEDED (simulated → live)

Everything **you** must set up to move arbiter from simulated to live. Updated as the build progresses.
Until each item is done, the corresponding feature runs in **simulated / shadow / stub** mode (honest status
surfaced in the dashboard + CLI). Nothing here blocks the paper-sim MVP from running.

| # | Item | Needed for | Status |
|---|------|-----------|--------|
| 1 | **Alpaca paper account + API keys** (`ALPACA_API_KEY`, `ALPACA_SECRET_KEY`, paper base URL) | Real market data via the Alpaca data API + (optional) paper execution | ✅ **set & verified 2026-06-19** — paper account ACTIVE ($100k), `/v2/account` + data bars both 200. Real AAPL bars flowing. Still `SimExecutor` while `LIVE_TRADING=false`. |
| 2 | **Stooq access** (no key; just confirm reachable) | Backup/price history for delisted tickers (survivorship) | ⬜ verify (Alpaca is now primary; Stooq is the fallback) |
| 3 | **SEC EDGAR fair-access compliance** (declared User-Agent w/ contact email) | Form 4 + 13D/13G ingestion. NOTE: when `EDGAR_USER_AGENT` is unset the whole EDGAR lane goes **inert with one WARNING** — it does **not** crash the cycle. The ingest runner guards on the User-Agent and skips Form-4/13D-G (logging `edgar.disabled_no_user_agent` / `form4_skipped_no_user_agent`); the `EdgarClient.from_config_or_none(...)` factory returns `None`. (The bare `EdgarClient(...)` constructor still raises `ValueError` for back-compat, but the live path never hits that — it is guarded.) | ✅ **set & LIVE-VERIFIED 2026-06-19** — `EDGAR_USER_AGENT="Jonathan Morris heritagehousepainting@gmail.com"`. Real AAPL CIK resolved, Form-4 + 13D/13G discovered, fetched, parsed, normalized, and **persisted** (live ingest: 40 fetched → 13 written; DB now form4:3, form13d:10). Three live-only bugs found+fixed during verification (over-strict primary-doc sanitizer, padded-CIK 301, XSL-rendered-vs-raw-XML) — all locked behind offline regressions. |
| 4 | **Kill-switch host (off-box)** — a tiny always-on endpoint the broker-side kill switch reads | **WIRED**: the engine calls `KillSwitch.is_halted()` before every cycle and **fails closed** — if `KILL_SWITCH_URL` is unset or unreachable, the engine HALTS (no orders). Set a real URL to operate normally | ✅ **DEPLOYED + VERIFIED 2026-06-21** — Cloudflare Worker at `https://stockbot.heritagehousepainting.workers.dev` (returns `{"halted":false}`, 200). `KILL_SWITCH_URL` set in `.env`. Verified: live worker → ALLOWED; unreachable → HALTED; empty → HALTED (fail-closed all confirmed). **To emergency-halt:** set Worker var `HALTED=true` + redeploy. (Explicit-halt live test still optional — flip HALTED=true once to watch arbiter halt.) |
| 5 | **Alert channel** (push/webhook — ntfy/Pushover/Slack webhook URL) | Critical alerts → webhook POST + durable auto-pause. Critical auto-pause IS wired into the cycle: kill-switch halted, any tripped breaker, broker-fatal error on a BUY/exit-SELL submit, AND (added 2026-06-21) a broker account-read failure all fire a critical alert + latch `engine.paused`. | ✅ **set & LIVE-VERIFIED 2026-06-19** — `ALERT_WEBHOOK_URL=https://ntfy.sh/arbiter-alerts-REDACTED` (private ntfy topic). A real critical alert delivered (`webhook_delivered status=200`). **You MUST subscribe** to that topic in the ntfy app to receive them. KNOWN GAP (post-session): reconciler ledger/broker divergence fires `tier=warning` → audit-log only, NOT the phone; watch `tail -f data/audit.jsonl` during a supervised session. Runtime watchdogs (`health.run_watchdogs`) exist but are not yet wired into the daemon loop — wire before UNATTENDED `daemon` use. |
| 6 | **MiroFish A2 service — WE BUILT OUR OWN** (`/Users/jonathanmorris/poly_bot/mirofish/`, our code, not the external AGPL repo) | Phase 4 A2 advisor over local HTTP. Arbiter side **WIRED (Wave 2)**: `A2.mirofish` per-idea list-valued channel into fusion; no-op while `MIROFISH_ENDPOINT` unset. **The service now EXISTS + is END-TO-END PROVEN (2026-06-21):** hybrid brain = deterministic evidence (Alpaca technical + SEC `companyfacts` fundamentals, PIT `filed<=as_of`) → Claude `claude-sonnet-4-6` tool-use → 2 opinions (short technical ~10d + medium fundamental ~60d). 94 tests green; arbiter's real `adapter.run` got a **negative MEDIUM stance** back over loopback. To ACTIVATE: (a) set **`ANTHROPIC_API_KEY`** in env (the ONLY new key — without it runs `MIROFISH_FAKE_LLM=1` with canned judgment; fundamentals reuse `EDGAR_USER_AGENT`, bars reuse `ALPACA_*`, all already set); (b) launch `cd /Users/jonathanmorris/poly_bot && arbiter/.venv/bin/python -m mirofish` (binds 127.0.0.1:8900); (c) set `MIROFISH_ENDPOINT=http://127.0.0.1:8900` in `arbiter/.env`. Note: `valuation_z` is a coarse vendored static sector-PE heuristic (v2 = live peers). | ⬜ set `ANTHROPIC_API_KEY` + launch + set `MIROFISH_ENDPOINT` |
| 6b | **Schedule 13D/13G activist ingest** (`A1.activist`, `source="form13d"`) | **WIRED (Wave 2)**: detection → emit (180d LONG; bearish stance on a 13D/G exit) → engine → runner. Uses the **same `EDGAR_USER_AGENT`** as Form 4 (#3) — inert with a warning until that UA is set | ✅ **LIVE-VERIFIED 2026-06-19** — real AAPL 13G persisted (e.g. Berkshire Hathaway 21% stake parsed → `source=form13d, txn_type=P`). Covered by #3's UA. |
| 6c | **Partial-fill realized-notional fold** | **WIRED (Wave 2)**: on an Alpaca **partial** fill the engine now folds the *realized* notional (`avg_fill_price × filled_qty`) into the risk book instead of the full requested notional. **Offline-tested but NOT live-verified** — it is **dormant under `SimExecutor`** (which always fills fully) and only activates under real Alpaca partials | ⬜ verify against a real Alpaca partial fill |
| 7 | **X/news paid API decision** (which provider, budget) | Phase 6 A3 tip layer | 🔒 Phase 6 |
| 8 | **IBKR account + gateway** | Phase 8, only if Alpaca limits bind | 🔒 Phase 8 |
| 9 | **`--approve-live` sign-off** (manual, expires every 30 days) | Phase 7 paper→live gate (also requires 60d/≥30 trades/Sharpe≥1.0/DD≤8%) | 🔒 Phase 7 |

**Legal:** trading on public disclosures for **personal use** is fine; managing others' money triggers RIA
rules — stay personal-use. (Recorded from the design spec.)
